rules_version = '2';

target /databases/{database}/documents {
  // Allow write access to event registration data
  match /eventRegistrations/{registrationId} {
    allow write: if request.auth != null; // Only authenticated users can write
  }
}